/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPartyAccountPda } from "../pdas";
import { RATE_MY_POLITICIAN_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_PARTY_DISCRIMINATOR = new Uint8Array([
  139, 249, 155, 19, 94, 156, 242, 167,
]);

export function getInitializePartyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_PARTY_DISCRIMINATOR,
  );
}

export type InitializePartyInstruction<
  TProgram extends string = typeof RATE_MY_POLITICIAN_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPartyAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPartyAccount extends string
        ? WritableAccount<TAccountPartyAccount>
        : TAccountPartyAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializePartyInstructionData = {
  discriminator: ReadonlyUint8Array;
  partyId: string;
};

export type InitializePartyInstructionDataArgs = { partyId: string };

export function getInitializePartyInstructionDataEncoder(): Encoder<InitializePartyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["partyId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_PARTY_DISCRIMINATOR }),
  );
}

export function getInitializePartyInstructionDataDecoder(): Decoder<InitializePartyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["partyId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getInitializePartyInstructionDataCodec(): Codec<
  InitializePartyInstructionDataArgs,
  InitializePartyInstructionData
> {
  return combineCodec(
    getInitializePartyInstructionDataEncoder(),
    getInitializePartyInstructionDataDecoder(),
  );
}

export type InitializePartyAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPartyAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  partyAccount?: Address<TAccountPartyAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  partyId: InitializePartyInstructionDataArgs["partyId"];
};

export async function getInitializePartyInstructionAsync<
  TAccountAuthority extends string,
  TAccountPartyAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RATE_MY_POLITICIAN_PROGRAM_ADDRESS,
>(
  input: InitializePartyAsyncInput<
    TAccountAuthority,
    TAccountPartyAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializePartyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPartyAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RATE_MY_POLITICIAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    partyAccount: { value: input.partyAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.partyAccount.value) {
    accounts.partyAccount.value = await findPartyAccountPda({
      partyId: expectSome(args.partyId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.partyAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializePartyInstructionDataEncoder().encode(
      args as InitializePartyInstructionDataArgs,
    ),
    programAddress,
  } as InitializePartyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPartyAccount,
    TAccountSystemProgram
  >);
}

export type InitializePartyInput<
  TAccountAuthority extends string = string,
  TAccountPartyAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  partyAccount: Address<TAccountPartyAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  partyId: InitializePartyInstructionDataArgs["partyId"];
};

export function getInitializePartyInstruction<
  TAccountAuthority extends string,
  TAccountPartyAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RATE_MY_POLITICIAN_PROGRAM_ADDRESS,
>(
  input: InitializePartyInput<
    TAccountAuthority,
    TAccountPartyAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializePartyInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountPartyAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RATE_MY_POLITICIAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    partyAccount: { value: input.partyAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.partyAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializePartyInstructionDataEncoder().encode(
      args as InitializePartyInstructionDataArgs,
    ),
    programAddress,
  } as InitializePartyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPartyAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitializePartyInstruction<
  TProgram extends string = typeof RATE_MY_POLITICIAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    partyAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializePartyInstructionData;
};

export function parseInitializePartyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializePartyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      partyAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializePartyInstructionDataDecoder().decode(instruction.data),
  };
}
